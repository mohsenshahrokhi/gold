# 📊 راهنمای انتخاب نماد و استراتژی

## ✨ ویژگی‌های جدید

### 1. انتخاب نماد در راه‌اندازی
- ربات به صورت خودکار نمادهای موجود در بروکر را بررسی می‌کند
- چهار نماد اصلی پشتیبانی می‌شود:
  - **XAUUSD** (Gold)
  - **EURUSD** (Euro/USD)
  - **US30/YM** (Dow Jones)
  - **BTCUSD** (Bitcoin)

### 2. تطبیق خودکار نام نمادها
- ربات نام واقعی نماد را در بروکر پیدا می‌کند
- پشتیبانی از پسوند/پیشوند (مثلاً `XAUUSD.`, `GOLD`, `XAU/USD`)
- فقط نمادهای قابل معامله نمایش داده می‌شوند

### 3. انتخاب استراتژی
- 5 استراتژی مختلف قابل انتخاب:
  1. **Professional Scalping Bot** (توصیه می‌شود)
  2. **Ultra Fast Scalping Bot**
  3. **Optimized NDS Bot**
  4. **Enhanced NDS Bot**
  5. **Original NDS Bot**

## 🔧 نحوه کار

### SymbolResolver
کلاس `SymbolResolver` مسئول پیدا کردن نام واقعی نمادها است:

```python
resolver = SymbolResolver()
real_symbol = resolver.find_symbol('XAUUSD')  # ممکن است 'GOLD' یا 'XAUUSD.' برگرداند
```

### الگوهای جستجو
برای هر نماد، چندین الگوی احتمالی تعریف شده:

- **XAUUSD**: `XAUUSD`, `GOLD`, `GOLDUSD`, `XAU/USD`, `GOLD/USD`, `XAUUSD.`, `GOLD.`
- **EURUSD**: `EURUSD`, `EUR/USD`, `EURUSD.`, `EUR/USD.`
- **US30**: `US30`, `YM`, `YM.`, `US30.`, `DOW`, `DOW.`, `US30USD`, `YMUSD`
- **BTCUSD**: `BTCUSD`, `BTC/USD`, `BTCUSD.`, `BTC/USD.`, `BITCOIN`, `BITCOINUSD`

### الگوریتم جستجو
1. **جستجوی دقیق**: ابتدا نام دقیق جستجو می‌شود
2. **جستجوی case-insensitive**: بدون توجه به حروف بزرگ/کوچک
3. **جستجوی partial match**: برای پسوند/پیشوند
4. **امتیازدهی**: بهترین تطابق انتخاب می‌شود

## 📝 مثال استفاده

```python
# دریافت منوی نمادها
symbol_menu = get_symbol_menu()
# {'XAUUSD (Gold)': 'GOLD', 'EURUSD (Euro/USD)': 'EURUSD', ...}

# انتخاب نماد
selected_symbol = symbol_menu['XAUUSD (Gold)']  # 'GOLD'
```

## ⚠️ نکات مهم

1. **MT5 باید اجرا باشد**: قبل از راه‌اندازی ربات، MT5 terminal باید باز و login باشد
2. **نمادهای غیرفعال**: نمادهایی که trade mode = 0 یا 2 دارند نمایش داده نمی‌شوند
3. **پسوند بروکر**: اگر بروکر شما پسوند دارد (مثلاً `.` یا `_`), ربات به صورت خودکار پیدا می‌کند

## 🔍 عیب‌یابی

اگر نمادی پیدا نشد:
1. بررسی کنید که MT5 terminal باز و login باشد
2. بررسی کنید که نماد در Market Watch موجود باشد
3. بررسی کنید که نماد قابل معامله باشد (trade mode)
4. لاگ‌ها را بررسی کنید برای جزئیات بیشتر

